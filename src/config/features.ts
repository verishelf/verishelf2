// Feature flags for gradual migration to Core API
// Toggle between Core API and legacy Supabase/inline implementations.
// NOTE: Do NOT remove Supabase usage yet – migration is incremental.

export const USE_CORE_API = true;

